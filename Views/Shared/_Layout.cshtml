<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - BlogIT</title>
    <link href="~/Content/Site.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />

    <link href="/Content/PagedList.css" rel="stylesheet" type="text/css" />
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/home/index">BlogIT</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    @*
                            @if (Request.IsAuthenticated)
                            {
                                <li class="nav-item">
                                    @Html.ActionLink("Applied Jobs", "getjobsbyuser", new { Controller = "Home" }, new { @class = "nav-link" })
                                </li>
                                <li class="nav-item">
                                    @Html.ActionLink("Your Posts", "getjobsbypublisher", new { Controller = "Home" }, new { @class = "nav-link" })
                                </li>
                                <li class="nav-item">
                                    @Html.ActionLink("Categories", "Index", new { Controller = "Categories" }, new { @class = "nav-link" })
                                </li>

                            }
                        }

                        <li class="nav-item w-auto">
                            @using (Html.BeginForm("search", "Home", FormMethod.Post))
                            {
                                <input class="form-control" type="text" placeholder="Search" name="searchName" id="searchName">
                            }

                        </li>*@

                </ul>
                @Html.Partial("~/Views/Shared/_LoginPartial.cshtml")

            </div>
        </div>
    </nav>
    <div class="mb-3 d-flex flex-column" style="min-height: 84vh">
        @RenderBody()
    </div>
    <footer class="border-top mt-auto">
        <p class="text-center mt-3 ">&copy; @DateTime.Now.Year - My ASP.NET Application</p>
    </footer>

    <script src="~/Scripts/jquery-3.6.0.js"></script>
    <script src="~/Scripts/bootstrap.js"></script>
    <script src="~/Scripts/jquery.validate.min.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
</body>

</html>
